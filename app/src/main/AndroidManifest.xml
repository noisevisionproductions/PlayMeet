<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">

        <activity
            android:name=".PostsManagement.PageWithPosts.UsersActivePosts"
            android:theme="@style/noBar"
            android:exported="true" />
        <activity
            android:name=".UserManagement.UserAccountLogic"
            android:theme="@style/noBar"
            android:exported="true" />
        <activity
            android:name=".PostsManagement.PostsOfTheGames"
            android:theme="@style/noBar"
            android:exported="true" />
        <activity
            android:name=".PostsManagement.PostCreatingLogic"
            android:theme="@style/noBar"
            android:exported="true" />
        <activity
            android:name=".MainMenu"
            android:theme="@style/AppTheme"
            android:exported="true" />
        <activity
            android:name=".LoginRegister.LoginAndRegisterActivity"
            android:theme="@style/AppThemeForLoginAndRegister"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginRegister.MainActivity"
            android:theme="@style/AppTheme">

        </activity>
    </application>

</manifest>